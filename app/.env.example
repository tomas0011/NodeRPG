# URL del backend (Express en Render). En desarrollo local apunta al backend local.
# En Vercel: setear esta variable con la URL pública del servicio de Render,
# p. ej. https://noderpg-backend.onrender.com  (SIN barra final).
REACT_APP_API_URL=http://localhost:3001
