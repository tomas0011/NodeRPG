import Sala from "./Sala";

/**
 * Sala del jefe: contiene un enemigo fuerte (p. ej. `Ogro`) con stats y botín
 * altos. Suele ser el final del recorrido del mapa fijo. Su población sale del
 * `MapaLayout`.
 */
export default class SalaDelJefe extends Sala {}
