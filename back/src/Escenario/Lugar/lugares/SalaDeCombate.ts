import Sala from "./Sala";

/**
 * Sala de combate: contiene enemigos (p. ej. `Bandido`) que el jugador debe
 * derrotar con `atacar`. Su población sale del `MapaLayout`.
 */
export default class SalaDeCombate extends Sala {}
